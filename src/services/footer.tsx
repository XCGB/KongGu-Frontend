// 自定义底部组件
import React from "react";

const CustomFooter: React.FC = () => {
  return (
    <div style={{ height: '60px', backgroundColor: '#f0f0f0' }}>
      {/* 底部内容 */}
    </div>
  );
};
export default CustomFooter;
